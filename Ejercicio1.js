//1.Crear una función power que recibe dos argumentos base y exponent. La función debe retornar el resultado
//de elevar base a la potencia exponent

function power(base, exponent){
    return Math.pow(base,exponent);
 }

 var Resultado = power(9,4);

 console.log(Resultado);